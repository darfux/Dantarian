<%= form_for(@book_info) do |f| %>
  <% if @book_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book_info.errors.count, "error") %> prohibited this book_info from being saved:</h2>

      <ul>
      <% @book_info.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :isbn %><br>
    <%= f.text_field :isbn %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :cover %><br>
    <%= f.text_field :cover %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
