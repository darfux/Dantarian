<!-- aio means All In One -->
<div id="header" ng-class="{inputing: input.focus, correct: aio_form.input.$valid, error: !input_is_correct(), returning: ret_book}">
	<div id="user-info">
		<div id="nickname"><%= @current_user.nickname %></div>
		<div id="account"><%= @current_user.account %></div>
	</div>

</div>
{{book}}
<div id="content">
	<div id="borrow-bar">
		<form name="aio_form" novalidate>
			<div id="borrow_input">
				<%= text_field_tag :input, nil, {id: 'aio-input', "ng-model": "aio.input", 'all-in-one': '', required: '',spellcheck: false, "ng-class": "{inputing: input.focus, correct: input_is_correct(false), error: !input_is_correct()}", autocomplete: "off", "ng-focus": "input.focus=true;", "ng-blur": "input.focus=false", "ng-paste": "handle_paste()"}%>
				<span id="clear-btn" ng-show="aio_form.input.$viewValue" ng-click="handle_clear()">x</span>
			</div>
			<div id="search-popup" ng-show="input_is_correct(false)">
				<div ng-show="book.searching" class="loading">Loading...</div>
				<div ng-show="book.show" id="book-show">
				<div id="book-cover">
					<%= image_tag("", {"ng-src": "{{book.cover}}"}) %>
				</div>
				<div id="book-info">
					<p>{{book.name}}</p>
					<p>{{book.author}}</p>
					<span id="response-msg" ng-class="{ok: 'success', fail: 'error'}[book.borrow_state]">{{response_msg}}</span>
					<div id="operation-button-group">
						<div id="borrow-btn" class="btn" ng-show="btn.borrow"><button ng-click="handle_borrow($event)" >借阅</button></div>				
						<div id="ok-btn" class="btn" ng-show="btn.known"><button ng-click="ok_btn=false;handle_clear()">知道了</button></div>
						<div id="fav-icon" ng-show="btn.wish" ng-class="{favored: book.favored}"><ng-md-icon icon="favorite" size="35" title="加入愿望单"   ng-click="handle_favor()"></ng-md-icon></div>
					</div>

				</div>
				</div>
			</div>
		</form>
	</div>

	<div id="borrowed-list"ng-init="borrowed_books=<%= @current_user.borrowed_books.to_json %>">
	<div ng-repeat="book in borrowed_books" class="list-cell" ng-class="{fade: ret_book==book.id}">
		<img ng-src="{{book.cover}}">
		<span class="name">{{book.name}}</span>
		<span class="author" title="{{book.author}}">{{author_brief(book)}}</span>
		<span class="isbn">{{book.isbn}}</span>
		<span class="retbook_btn" title="还书请按住我"><button ng-mousedown="handle_ret($event, book.id)" ng-mouseup="handle_cancel_ret()"></button></span>
	</div>
	</div>
</div>






